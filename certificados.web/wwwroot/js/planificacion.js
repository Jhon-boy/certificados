$(document).ready(function () {
    $('#planificacion-form').on('submit', function (e) {
        e.preventDefault();
        alert('Formulario guardado exitosamente.');
        // Puedes agregar lógica para enviar los datos al servidor aquí.
    });
});
